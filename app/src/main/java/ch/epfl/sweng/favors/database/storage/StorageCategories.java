package ch.epfl.sweng.favors.database.storage;

public enum StorageCategories {
    FAVOR{
        @Override
        public String toString() {
            return super.toString().toLowerCase();
        }
    },
    PROFILE{
        @Override
        public String toString() {
            return super.toString().toLowerCase();
        }
    }
}
