<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.epfl.sweng.favors">

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        >
        <activity android:name=".FavorsMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AuthentificationProcess"
            android:parentActivityName=".FavorsMain" />
        <activity
            android:name=".Logged_in_Screen"
            android:label="@string/title_activity_logged_in__screen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ConfirmationSent"
            android:label="@string/title_activity_confirmation_sent" />
        <activity android:name=".SetUserInfo" />
        <activity
            android:name=".TestActivity"
            android:label="@string/title_activity_test"/>
    </application>

</manifest>